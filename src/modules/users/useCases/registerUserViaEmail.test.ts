describe("Registering a user via email", () => {
  // test for already existing email
  // test for already existing username
  // test that User.create is called with the right params
  it("Should pass", () => expect(true).toBeTruthy());
});
